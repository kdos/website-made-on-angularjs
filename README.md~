Website made on Angular-seed boilerplate. Used angularjs, Materialize -material design based framework by google ,nodejs, jasmine and karma -test runner.

First 
#npm install

then to run the application
#npm start

to run 
